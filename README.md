# java-project-
Users can perform CRUD operations on student data, such as adding new students, reading student details, updating student information, and deleting student records. The project utilizes core Java for logic implementation and Servlet HTML/CSS (Java Database Connectivity) for database interaction with UX and UI. Upload solution
